# libc++ KASan Test program

This program tests the container instrumentation by libc++ with
Kernel Address Sanitizer enabled.

Note that when running the program under QEMU a VNC viewer is necessary
to observe the error messages.